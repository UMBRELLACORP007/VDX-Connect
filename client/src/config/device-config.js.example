// Fill this in per-machine. This file is NOT committed with real secrets —
// copy device-config.example.js to device-config.js and put real values in,
// device-config.js is gitignored.
//
// deviceId/secret must match one of the two pairs in the server's .env

module.exports = {
  deviceId: '-----------', // or 'device-friend' on the other machine
  secret: '----------------------',
  serverUrl: 'https://--------------', // switch to your Railway URL once deployed

  // Metered TURN credentials — same on both machines, from dashboard.metered.ca
  turnUsername: '------------',
  turnPassword: '-------',
};
